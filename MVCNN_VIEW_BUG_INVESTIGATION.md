# MVCNN 視角 bug 調查紀錄（2026-09-18）

一個單行的閉包 bug 引出的連鎖調查。這份文件記錄**動機、做過的實驗、以及每一步的證據**，
包含被推翻的結論。目的是讓接手的人知道哪些事已經驗證過、哪些還是猜測、哪些我曾經講錯。

摘要式的操作結論在 `MODEL_PIPELINE_HANDOVER.md` §13.0–§13.0c，這裡是完整脈絡。

---

## 0. 重點摘要

| 發現 | 狀態 |
|---|---|
| `MVCNN_Siamese` 三個視角都只讀第 3 張圖 | ✅ 確證 |
| 修正後重訓**沒有變好** | ⚠️ **已撤回**——基線不可信，見 §5 |
| 存檔的舊 annotator 不能當基線（夾帶約 0.02 AUC 偏差） | ✅ 確證 |
| 三視圖裡第 1 張幾乎沒有資訊，第 2、3 張高度冗餘 | ✅ 確證 |
| augmentation 有 60% 的樣本是物理上不存在的視角組合 | ✅ 確證 |
| 修正 augmentation 幾何能改善模型 | ⛔ **已推翻**——三折合併 +0.0006 [−0.0142, +0.0154] |
| ~~真正該做的不變性是 8 個「特徵向量符號翻轉」~~ | ⛔ **已推翻**，前提錯誤，見 §0a |
| FC 和 EM 各有一組 BN，破壞了 Siamese 對稱性 | ✅ 確證；但共用 BN **改善不了**，主因在 head |
| val_loss 對三視角模型不是好的模型選擇依據 | 🔶 證據弱但方向明確 |
| 加 pseudo label 資料量沒有在起作用 | 🔶 有洩漏，參考用 |
| **專家標註量仍是瓶頸**，每倍增約 +0.04 AUC | ✅ 三折確證，見 §10b |

---


## 0a. ⛔ 2026-09-19 重大更正：視圖的座標系不是主軸，是標準腦座標

**§4 和 §7 的推理建立在一個錯誤前提上。**

模型訓練吃的是 `data/standard_views/`，由 **`standard_draw.py`** 產生，
而該檔第 2 行寫明「**使用標準腦座標畫圖，不需要讀取 descriptor 中的 centroid 和 eigvecs**」
（全檔一次都沒用到 eigvecs）。也就是說：

- view0 = **YZ 平面**、view1 = **XZ**、view2 = **XY**，全部是**標準腦座標下固定的解剖學切面**，
  對每一顆神經元都一樣，**完全沒有旋轉到主軸座標系**
- 每顆神經元只是各自置中、按自己的**立方體** bbox 縮放（`side = max(dx,dy,dz)`）

用主軸旋轉的是 `pair_draw.py`（產生 pair-aligned 的資料），**但 CNN 沒有吃那份**。

### 因此失效的推理

| 原本的論證 | 狀態 |
|---|---|
| `np.linalg.eigh` 特徵向量正負號任意 → 必須對 8 個符號翻轉不變（§7） | ❌ **不適用**，標準腦座標沒有特徵分解 |
| 軸順序由特徵值降序決定 → 重排的組態測試時不會出現（§7） | ❌ **不適用** |
| 18.4% 特徵值接近簡併 → 軸 0↔1 不穩（§7） | ❌ **與 CNN 輸入無關** |
| 前處理把姿態標準化，轉 90° 破壞它（§4） | ❌ **不適用** |
| 三視圖共用座標軸 → 3D 旋轉 = 視角重排 + 各視角 2D 操作 | ✅ **仍成立**，只需要三個正交投影 |

`view_geometry.py` 那張 48 個變換的對應表**仍然正確**，錯的是我用來挑子集的理由。

### 連帶的第二個錯誤：主軸方向講反了

慣性張量對軸 u 的特徵值 = Σ m·(點到 u 軸的垂直距離平方)，所以**特徵值最小的軸才是最細長的方向**。
降序排列下，軸 2（λ 最小）是最長的延伸方向，軸 0（λ 最大）延伸最短。
實測 6 顆神經元的延伸量：沿軸 0 約 5.7–20.0、沿軸 2 約 30.2–62.2。
我在 §7 寫的「繞**最長**主軸的 C4」實際上繞的是最短的那一軸。

### 這造成了什麼後果

按 §7 挑出的「8 個符號翻轉」在影像空間**只有 0° 和 180° 旋轉，完全沒有 90°/270°**
（因為「不重排視角」與「含 90° 旋轉」在 48 個變換中是互斥的：40 個含 90° 的全部重排視角，
8 個不重排的全部只有 0°/180°）。而這個模型的主要正則化來源就是 augmentation，
結果 10 折基線的守門 AUC 從 0.9293 掉到 0.8140（−0.1154 [−0.1377, −0.0932]）。
⚠️ 但那一版同時中了 §0b 的 batch 標籤洩漏（8 符號翻轉 × 2 交換 = 16 = batch size），所以這個落差**不能**單獨歸因於拿掉 90° 旋轉，待重測。
詳見 §13。

### 修正後的理解

標準腦座標下沒有座標系歧義，augmentation 的作用回到**單純的正則化**。
又因為完全共用正規化層 + max pooling 讓模型**精確置換不變**（實測交換視角差 0），
跨視角的配置對模型不可見（實測：三視角獨立抽操作 vs 統一施加，擾動幅度比 1.02x）。
所以只需要最大化**每張圖的 in-plane 變換分布**——即 D4 的 8 個無損操作。


## 0b. ⛔ 2026-09-19 第二個重大 bug：batch 內標籤洩漏

`data_process_train_v2.py` 第一版的 10 折基線 pooled 測試 AUC 只有 **0.6667**
（舊 annotator 0.9293）。原因不是架構，是資料管線：

```python
ds = ds.shuffle(...)        # ← 只在「對」的層級洗牌
ds = ds.flat_map(expand)    # ← 每對連續吐出 AUG_PER_PAIR 筆
ds = ds.batch(batch_size)
```

`AUG_PER_PAIR = 2 × 8 = 16`，而 `batch_size = 16`。兩者**剛好相等**，所以
`batch()` 的切點與 `flat_map` 的邊界完全對齊：**每個訓練 batch 正好是同一對
神經元的 16 個擴增，批內標籤必然全同**。實測每批來自 1.00 對。

BatchNorm 在訓練時用該批自己的統計量。批內標籤全同 ⇒ 批統計量本身就是標籤的
函數 ⇒ 模型可以完全繞過影像內容。這是經典的 BN 標籤洩漏。

### 證據

| 量測 | 洩漏版 | 說明 |
|---|---|---|
| 訓練時回報的 `auc` | 0.9987 | 走捷徑的假象 |
| 同一權重、推論模式、**訓練集** | 0.8675 | 真實擬合程度其實更差 |
| 推論模式 val / test | 0.7496 / 0.5938 | |
| 強制用 batch 統計量（training=True）val | 0.4729 | 捷徑在小批次外不成立 |
| 舊 Annotator3v（10 擴增，未對齊） train/val/test | 0.9925 / 0.9956 / 0.8924 | 對照 |

排除過的其他解釋：BN moving 統計量失配（訓練模式反而更差，不是失配）；
max pooling 被單一視角壟斷（洩漏版 v0 佔 0.573，但舊版是 v2 佔 0.554，
方向相反且兩者都不極端）。

### 為什麼之前的實驗沒中

legacy augmentation 是 `2 × (4 旋轉 + 1 翻轉) = 10` 筆/對，與 batch 16 不整除，
batch 邊界會跨過對的邊界，每批混到 1–2 對。那是**部分**洩漏，不是完全洩漏，
所以 2×2 實驗那批（0.89–0.93）看起來正常。

`physical` 模式同樣是 5 spec × 2 = 10 筆，也沒中。

### 這解釋了先前誤判的 −0.1154

§0a 把「8 個符號翻轉」版基線掉到 0.8140 歸因於拿掉了 90°/270° 旋轉。
但 8 符號翻轉 × 2 交換 = **16**，同樣與 batch 16 對齊，同樣完全洩漏。
那 0.1154 的落差至少有一部分是洩漏造成的，**不能**當成「90° 旋轉有效」的證據。
這個推論作廢，要重測。

### 修正

在 `flat_map` 之後補一次樣本層洗牌：

```python
ds = ds.flat_map(expand)
ds = ds.shuffle(2048, seed=cfg.seed + 1, reshuffle_each_iteration=True)
```

buffer 2048 = 128 對。修正後每批混到 13–16 對。效果立即可見：
修正前跑滿 300 epochs 的 val_auc 停在 0.70；修正後**第 1 個 epoch** val_auc
就是 0.8726，第 4 個 0.9130。

### 教訓

**永遠要在擴增之後再洗一次牌**。擴增倍數與 batch size 的整除關係只決定洩漏是
「完全」還是「部分」——補上樣本層洗牌之後兩者都回到 0%，這個關係就不再重要，
batch size 可以純粹按訓練動力學來選。
`Data_process_Train.make_tf_dataset`、`data_process_fineTune.py`、
`data_process_preTrain.py` 都是同樣的結構，已於 2026-09-19 一併修正
（`Data_process_Train` 留了 `NRN_LEAK_REPRO=1` 只為了量這個 bug 的代價）。

---

## 1. 起點：一行閉包 bug

`model.MVCNN_Siamese` 的視角切片原本寫成：

```python
views = [Lambda(lambda x: tf.expand_dims(x[..., i], axis=-1))(input_image)
         for i in range(3)]
```

Python 的閉包是**延遲綁定**的：三個 lambda 共用同一個 `i`，等到模型執行時 `i` 已經是 2。
所以三個「視角」全部讀到 channel 2，另外兩張對輸出完全沒有影響。
MVCNN 的 view pooling 變成對三份**相同**的特徵取最大值，等於沒有作用。

### 證據

1. **通道清零**：把 channel 0、1 清零，舊模型輸出變化**恰好 0.0000**；清 channel 2 變化 0.3348。
2. **BN 統計量**：三個逐視角 BN 的 `moving_mean` / `moving_var` **逐位元相同**。
   （gamma/beta 不同，是各視角後面獨立的 Dropout 造成的，符合預期。）
3. **視角消融**（專家測試集 AUC，三折平均）：

   | | 全部 | 去掉第1張 | 去掉第2張 | 去掉第3張 | 只留第3張 |
   |---|---|---|---|---|---|
   | 舊模型 | 0.9239 | 0.9239 | 0.9239 | **0.5000** | **0.9239** |

   去掉第 3 張輸出變常數（AUC 0.5），只留第 3 張就等於全部。

### 影響範圍

所有既有權重都是在這個狀態下訓練的（Annotator / PreTrain / FineTune / 線上服務）。
**訓練與推論行為一致，所以服務沒有錯**，只是從來沒用到 2/3 的視角資訊。
反過來**不能拿舊權重去配修正後的切片**：Annotator fold 0 守門 AUC 0.905 → 0.836、
fold 5 0.900 → 0.857、FineTune fold 0 0.962 → 0.875。

處理：`MVCNN_Siamese` 保留不動（只加註解），新增 `MVCNN_Siamese_3View`，
舊權重備份到 `Annotator_Model_backup_singleview_20260918/`。

---

## 2. 修正後重訓：沒有變好

`Annotator3v` = 舊 annotator 的設定 + 修正切片，其餘完全相同。fold 0/1/2：

| fold | 舊(存檔) | 新(三視角) | 差 [95% CI] |
|---|---|---|---|
| 0 | 0.9050 | 0.8924 | −0.013 [−0.058, +0.035] |
| 1 | 0.9510 | 0.9286 | −0.022 [−0.054, +0.011] |
| 2 | 0.9158 | 0.9099 | −0.006 [−0.061, +0.049] |
| 合併 | 0.9248 | 0.9083 | −0.017 [−0.041, +0.009] |

⚠️ **這個比較後來被證明是無效的，見 §5。** 但當時它驅動了接下來兩個調查方向。

### 為什麼沒變好？視角消融給了答案

新模型的消融（三折平均）：

| | 全部 | 去掉第1張 | 去掉第2張 | 去掉第3張 | 只留第1張 | 只留第2張 | 只留第3張 |
|---|---|---|---|---|---|---|---|
| 新模型 | 0.9103 | 0.9098 | 0.8672 | 0.8671 | **0.6570** | 0.8547 | 0.8691 |

- **第 1 張 =(y,z) 是高度冗餘的**（⚠️ 原文寫「兩個最小主軸，幾乎是空的」，兩處都錯：
  這是標準腦座標的 YZ 面不是主軸；而且它不是空的——單獨用有 0.657，明顯高於隨機的 0.5，
  只是資訊被另外兩張涵蓋）：去掉只掉 0.0005。它是**唯一不含腦座標 x 軸**的視圖，
  而 x 是 65.8% 的神經元延伸最長的方向；x 在 view1 與 view2 各出現一次，所以丟掉 view0 不損失。
- **第 2、3 張高度冗餘**：各自單獨就有 0.855 / 0.869，接近三張全用的 0.910。

**bug 意外選中的第 3 張，正好是三張裡最有資訊的一張。**

⚠️ 清零是分布外輸入（模型沒看過全黑的視角），只能當相對強弱參考，
不等於「拿掉該視角重新訓練」的結果。

---

## 3. concat 實驗：取消 view pooling 沒有幫助

動機：如果 max pooling 丟掉了跨視角資訊，保留視角身分應該有幫助。

| fold 0 | max | concat |
|---|---|---|
| 守門 AUC | 0.8927 | 0.8927 |
| 測試 f1 (閾值 0.5) | 0.864 | **0.725** |
| best val_loss | 0.0928 | **0.1046** |
| best 時 train_loss | 0.0714 | **0.0574** |
| 參數量 | 4.79M | **14.22M** |

AUC 完全相同（巧合，已驗證兩個模型的預測確實不同：相關 0.944、最大差 0.258；
AUC 只有 59×63=3717 個離散取值，剛好落在同一格）。

但 f1 掉了 0.14：**排序能力沒變，閾值位置跑掉了**。配合 `train_at_best` 更低、
`best_val` 更高，是典型的容量過剩過擬合——多出來的 9.4M 參數拿去記訓練集了。

> 附帶修掉一個 bug：`pool_type="concat"` 這條路徑原本**從來不能用**。
> 舊函數用 `Lambda(tf.reshape)` 讓靜態維度變成 `None`，後面 `Dense` 無法決定權重形狀。
> 新函數改用 `Reshape((-1,))`。因為專案一直用預設的 `"max"`，沒人踩到。

---

## 4. augmentation 的幾何是錯的

### 動機

消融顯示三個視角的資訊高度重疊。一個可能是模型根本學不到跨視角關係——
於是去檢查 augmentation 有沒有破壞視角之間的對應。

### 發現

`Data_process_Train.make_tf_dataset` 對 `(H, W, 3)` 直接呼叫 `tf.image.rot90` /
`flip_left_right`，也就是**對三個 channel 施加同一個 2D 操作**。

但三張視圖**共用座標軸**（view0=(y,z)、view1=(x,z)、view2=(x,y)，見 `standard_draw.py:460-465`；
⚠️ 原文誤引 `pair_draw.py`，那是 CNN 沒有使用的另一份資料，見 §0a），
所以 3D 旋轉不是把三張圖各自就地旋轉，而是「**視角重排 + 各視角自己的 2D 操作**」。
例如繞 z 轉 90°：view2 就地轉，view0 和 view1 **互換**（其中一個還要鏡射）。

`view_geometry.py` 用點雲數值反解立方體的 48 個對稱操作（不是手推），並用第二組點雲驗證：

| 現行操作 | 對應的 3D 變換 |
|---|---|
| rot0 | 恆等 |
| rot90 | **✗ 沒有任何 3D 變換能產生** |
| rot180 | 點反演 `-x,-y,-z`（鏡射） |
| rot270 | **✗ 沒有任何 3D 變換能產生** |
| rot0+flip | **✗ 沒有任何 3D 變換能產生** |

每個 base pair 展開成 10 筆（2 種 FC/EM 交換 × 5 個操作），
**其中 6 筆（60%）是物理上不存在的視角組合。**

**這對舊模型無害、對新模型才開始有害**：舊模型只讀一張圖，單張做任何 2D 旋轉翻轉
都是合法的 2D augmentation。

⛔ 原文此處還有一句「前處理特地把神經元對齊到主軸座標系來標準化姿態，轉 90° 等於把中間軸
擺到最大軸的位置」——**完全錯誤**，訓練資料用的是標準腦座標，沒有主軸對齊。見 §0a。

### 2×2 對照實驗

`aug_mode="physical"` 只用真實可達的變換（繞**軸 0** 的 C4 + 點反演；
⚠️ 原文寫「最長主軸」是錯的，軸 0 其實是延伸最短的那一軸，見 §0a），
結構與 legacy 的「4 旋轉 + 1 反射」相同、每對一樣 10 筆、583 steps/epoch 完全相同，
所以是乾淨的單變因對照。驗證方式是回到點雲搜尋，不是拿查表結果自我比對：
legacy 4/10 可達，physical 10/10 可達。

**預測**：max pooling 對視角重排較不敏感，所以修正對 concat 的增益應該大於對 max。

**結果（fold 0，n=122）**：

| pool | aug | 守門 AUC | best_val | best 時 train_loss |
|---|---|---|---|---|
| max | legacy | 0.8927 | 0.0928 | 0.0714 |
| max | physical | 0.8857 | 0.0931 | 0.0938 |
| concat | legacy | 0.8927 | 0.1046 | 0.0574 |
| concat | **physical** | **0.9204** | 0.0945 | 0.0545 |

- concat：legacy → physical **+0.0277 [+0.0016, +0.0566]**，P(較好)=0.982
- max：legacy → physical −0.0073 [−0.0423, +0.0251]，P(較好)=0.335

**增益來源的分解**：concat 的 `best_val` 和 `train_at_best` **同時**變好
= 資訊變好，不是正則化（正則化會是「訓練擬合變差、驗證變好」）。

### ⛔ fold 1 推翻了這個結論（2026-09-18）

fold 1 的 2×2 補完之後，**concat 的效應反號**：

| | max | concat |
|---|---|---|
| fold 0 physical − legacy | −0.0070 | **+0.0277** |
| fold 1 physical − legacy | +0.0025 | **−0.0207** |

fold 1 concat：0.9338 → 0.9131，差 −0.0203 [−0.0529, +0.0100]，P(physical 較好)=0.098。

**兩折合併（n=244）：0.9159 → 0.9172，差 +0.0014 [−0.0175, +0.0211]，P=0.556。完全是 null。**

所以 fold 0 那個 +0.0277（CI 下界 +0.0016、P=0.982）是**單折的偽陽性**。
每折測試集只有 122 對，這正是 §5 的複製對照所預告的失敗模式——
我在做出預測、又剛好在第一折看到符合預測的結果時，還是差點被說服。

**結論：augmentation 幾何不成立這件事是真的（§4 的證明沒有問題），
但「修正它能改善模型」沒有證據支持。** 幾何錯誤確實存在，只是似乎不影響最終效能。

⚠️ 記取的教訓：**單折 n=122 上，任何小於約 0.03 的 AUC 差異都不該當真**，
即使配對 bootstrap 的 CI 看起來排除了 0。先補折再下結論。

---

## 5. ⛔ 最重要的發現：存檔的舊 annotator 不能當基線

lr 掃描時加了一個「複製對照」：用**現在的程式碼**、和舊 annotator 完全相同的組態
（單視角架構、lr 1e-5、300 epochs、fold 0）重跑一次。

**結果 0.8849，存檔的是 0.9050，差 −0.0207 [−0.0651, +0.0202]。**

排除過的原因：
- split 檔內容自 2024 年未變（`git diff HEAD` 為空）
- `ddc284c`（專家信心 0.5 判為正例）只動到 `result_analysis*.py`，不碰訓練

剩下的解釋：存檔權重是 **2026-04-30**，早於 `e7a50a2`(05-10 全 pipeline 重構)、
`05cdd5c`(07-14)、`6e5994d`(07-16) 三個動到訓練路徑的 commit。

**所以拿存檔舊權重當基線，夾帶約 0.02 AUC 的未控制偏差，和我們在量的效果同一個量級。**
§2 那個「修正切片沒有變好」的結論因此撤回——對現在程式碼的 1v 基線 0.8849，
三視角的 0.8927 並沒有比較差。

**教訓：跨程式碼版本比較模型權重是無效的。要比較就要用同一份程式碼重跑對照組。**

---

## 6. lr 掃描：調 lr 救不回三視角模型

動機：懷疑 300 epochs / lr 1e-5 是為單視角調的，直接沿用對三視角不公平。
為了不讓調參只偏袒新模型，**兩種架構都掃**。

| lr | 1v 架構 | 3v 架構 |
|---|---|---|
| 1e-5 | 0.8849 | 0.8927 |
| 3e-5 | 0.8892 | **0.8929** |
| 1e-4 | 0.8641 | 0.8523 |
| 3e-4 | **0.8970** | 0.8483 |

- **lr 1e-5 對 3v 已接近最佳**（3e-5 只差 0.0002）。原本的假設不成立。
- epoch 數也不是瓶頸：checkpoint 取 best-val，舊模型 10 折的最佳 epoch 分布是
  22/30/30/42/118/212/229/230/270/277，沒有堆在 300 的邊界。

### ⛔ 附帶發現：val_loss 對三視角模型不是好的選擇依據

**`3v_lr3e-4` 的 val_loss 是 8 格裡最低的 0.0905，AUC 卻是 8 格裡最差的 0.8483。**

只看 1v 四格，val_loss 與 AUC 相關 Pearson −0.94（低 val_loss = 高 AUC，正常）；
只看 3v 四格是 +0.13（完全不相關）。每組只有 4 個點，相關係數本身證據很弱，
但上面那句事實就足以說明問題。

**影響**：`ModelCheckpoint(monitor="val_loss", save_best_only=True)` 對三視角模型
可能一直在挑錯的 checkpoint。可以改用 `tf.keras.metrics.AUC` + `monitor="val_auc"`，
但驗證集只有 165 對，AUC 在這個規模上抖動也很大，未必更好。
（順帶：現在的 `bi_acc` 指標是壞的——Keras `BinaryAccuracy` 拿 y_pred>0.5 去比
未二值化的 soft label，所以 log 裡會出現 0.41 這種數字。）

---

## 7. ⛔ 真正該做的不變性是什麼（**本節前提錯誤，結論作廢**）

> 本節整套推理建立在「視圖在主軸座標系」這個錯誤前提上，見 §0a。
> 保留原文供對照，但**不要據此設計 augmentation**。
> 唯一仍然成立的是 48 個變換的對應表本身（`view_geometry.py`）。

### 48 個對稱操作的乾淨切分

用 `view_geometry.py` 反解的表去數：

- **不重排視角的正好 8 個，而且正好就是純符號翻轉 `diag(±1,±1,±1)`。**
- **純粹只重排、完全不旋轉影像的有 0 個**（影像一定會轉，這點容易誤解）。

對上前處理的兩種狀態：

| | 前處理有沒有決定它 | 該不該做不變性 |
|---|---|---|
| 軸的**正負號**（8 個符號翻轉） | ❌ `np.linalg.eigh` 的符號是任意的，`swc_descriptor.py:78-84` 只排序不正規化符號 | **必須**——同一顆神經元重跑前處理就可能翻號，100% 受影響 |
| 軸的**順序**（其餘 40 個） | ✅ 按特徵值降序排定 | 這些組態測試時不會出現 |

所以「全部 24 個旋轉加鏡射」不只是效益不大，**其中一部分是有害的**：
教模型對軸順序不變，等於強迫它丟掉「view2 是（最長軸, 中間軸）平面」這個真實資訊。

### 一個例外：特徵值接近簡併

28612 顆 FC / 12767 顆 EM 的實測：

- **λ2/λ1 中位數 0.883，18.4% > 0.95** —— 最長軸和中間軸接近簡併，順序數值上不穩。
- **λ3/λ2 中位數 0.251，接近簡併的只有 0.26%** —— 最短軸幾乎總是分得很開。

所以合理的擴增群是 **8 個符號翻轉 × {恆等, 軸0↔1 互換} = 16 個**，不是 48，也不是 5。

### 對現有結果的誠實修正

目前跑的 physical 集合是 {identity, Rx90, Rx180, Rx270, −I}，其中 **Rx90/Rx270 交換的是
軸 1↔2**——按上面的分析正是「分得很開、沒有歧義」的那一類。
所以 §4 那 +0.0277 的增益，可能來自「移除 legacy 那 3 個不可能的操作」，
而不是來自加進去的 Rx90/Rx270。現在的設計分不開這兩者。

---

## 8. 逐視角 BN 破壞了 Siamese 對稱性

### 發現

`BatchNormalization()` 寫在 `process_single_view` 裡，而這個函數被呼叫兩次
（FC 一次、EM 一次），所以 **FC 和 EM 各有自己的三個 BN**（全模型 7 個 = FC 3 + EM 3 + head 1）。
conv 共享但 BN 不共享。

實測交換 FC/EM 後的輸出變化（訓練後的模型）：

| | 最大變化 | 平均變化 |
|---|---|---|
| 3v max | 0.154 | 0.104 |
| 3v concat | 0.429 | 0.223 |
| 1v 舊 | 0.484 | 0.161 |

在 [0,1] 的輸出上平均差 0.10–0.22。而 augmentation 特地做了 FC/EM 交換，
註解寫著「保正模型对输入有对称性」——**意圖是對稱，架構卻讓它不可能達成**。

### 和原始 MVCNN 的差異

原始 MVCNN（Su et al., ICCV 2015）**pooling 之前完全共享**：CNN₁ 對所有視角用相同權重，
view-pooling 之後才進 CNN₂。沒有任何 per-view 參數。這是該架構能置換不變、
能吃任意數量視角的前提。（原文用 VGG-M，那個年代是 LRN 不是 BN，但原則明確。）

**per-view BN 是偏離，一半合理一半是意外**：
- 合理：這裡的三個視角不是任意相機位置，而是按特徵值排序的主平面，統計分布本來就不同。
- 代價：置換不變性沒了。實測交換視角的輸出變化 max 0.043–0.068、concat 0.285–0.375——
  **max 的敏感度大約是 concat 的 1/5，但不是 0**（我一度說 max「完全不變」，那是錯的）。
- 意外：FC/EM 不共享那部分看起來不是設計選擇，是把 `BatchNormalization()`
  寫進一個會被呼叫兩次的函數造成的。

### 處理

`MVCNN_Siamese_3View` 新增兩個旗標，預設都關閉、既有權重相容：

| 旗標 | 作用 | BN 層數 | 參數量 | 交換 FC/EM 的差 |
|---|---|---|---|---|
| （預設） | 現況 | 7 | 4,786,657 | 8.1e-04 |
| `share_branch_norm=True` | FC/EM 共用逐視角 BN | 4 | 4,785,889 | 8.6e-04 |
| `sym_merge=True` | 對稱合併 head（和 + 絕對差） | 7 | 4,786,657 | **0** |

⚠️ 上表是**隨機初始化**下量的，此時網路輸出接近常數，測不出 `share_branch_norm`
對對稱性的改善。`sym_merge` 的 0 是結構性的，與權重無關。

### 結果：共用 BN **沒有**改善對稱性（2026-09-18）

`Annotator3vSharedBN` fold 0 訓練完成後，在真實測試資料上量交換 FC/EM 的輸出差：

| | 平均 \|差\| | 最大 |
|---|---|---|
| 各自 BN（Annotator3v） | 0.0377 | 0.1789 |
| 共用 BN（SharedBN） | **0.0373** | 0.1711 |

守門 AUC 0.8927 → 0.8937，差 +0.0011 [−0.0022, +0.0049]，P=0.712。**兩者都是 null。**

這反而確認了前面的結構分析：**不對稱的主要來源是 head 不是 BN**。
`concatenate([f_FC, f_EM])` 之後的 `Dense` 對前半後半用不同權重，
共用 BN 只移除了次要來源。要真的對稱得用 `sym_merge=True`（結構上保證，已實作未測）。

---

## 8a. 三折全部結果：**所有架構改動都是 null**（2026-09-18）

| 組態 | fold 0 | fold 1 | fold 2 | 逐折平均 |
|---|---|---|---|---|
| 1v 基線（單視角，現行程式碼） | 0.8849 | 0.9404 | 0.9231 | 0.9161 |
| 3v max（修正切片） | 0.8927 | 0.9286 | 0.9099 | 0.9104 |
| 3v max + physical aug | 0.8857 | 0.9311 | 0.9282 | 0.9150 |
| 3v concat | 0.8927 | 0.9338 | 0.9358 | 0.9207 |
| 3v concat + physical | 0.9204 | 0.9131 | 0.9355 | 0.9230 |

三折合併的配對 bootstrap（n=366）：

| 對比 | 差 [95% CI] | P |
|---|---|---|
| concat: legacy → physical | +0.0006 [−0.0142, +0.0154] | 0.525 |
| max: legacy → physical | +0.0093 [−0.0067, +0.0262] | 0.869 |
| 1v 基線 → 3v（修正切片） | −0.0085 [−0.0329, +0.0151] | 0.239 |
| 1v 基線 → 3v concat+physical | +0.0070 [−0.0127, +0.0271] | 0.746 |

**沒有任何一項達到顯著。** 折間變異（concat 從 0.8927 到 0.9358，差 0.043）
遠大於任何架構差異。physical 的效應逐折是 max −0.0070/+0.0025/+0.0184、
concat +0.0277/−0.0207/−0.0003，在兩個方向間亂跳。

### 這整輪調查的總結論

找到三個**真實存在**的缺陷，每一個都確證：

1. 視角切片只讀第 3 張（§1）
2. augmentation 幾何 60% 的樣本物理上不成立（§4）
3. FC/EM 分支各有一組 BN，破壞 Siamese 對稱性（§8）

**修好每一個都沒有可測量的效能改變。** 缺陷是真的，影響是量不到的。

**瓶頸不在架構。** 1097 對專家標註、每折測試集 122 對、複製誤差 ±0.02——
在這個資料規模下，架構層級的差異即使真的存在也量不出來。
下一步應該是資料縮放曲線（§10），而不是繼續改架構。

## 8b. 換 GPU 幾乎不影響結果（2026-09-18）

實驗搬到 backend-node（RTX 4090）之後，擔心跨 GPU 架構會引入新的混淆變因，
所以在兩台上各跑一次完全相同的組態（fold 2、max、legacy）：

**0.9099（compute-node / Blackwell）→ 0.9107（backend-node / Ada），
差 +0.0007 [−0.0030, +0.0044]，P=0.646。**

對照 §5 的「換程式碼版本」是 −0.0207 [−0.0651, +0.0202]。
**換 GPU 的影響比換程式碼版本小一個數量級**，所以跨節點分工是安全的
（但仍建議整個比較組留在同一台，成本很低）。

注意配對 bootstrap 的 CI 寬度差異（±0.004 vs ±0.045）本身就是訊息：
同程式碼跨 GPU 的兩個模型預測幾乎完全一致，跨版本的則是兩個真的不同的模型。

## 9. pseudo label 的資料量沒有在起作用

三個現成的預訓練模型，在全部專家配對上評估（n=1219）：

| 預訓練語料 | AUC [95% CI] |
|---|---|
| high_confidence | **0.9719** [0.9642, 0.9795] |
| 120K | 0.9332 [0.9183, 0.9464] |
| 180K | 0.9399 [0.9262, 0.9518] |

**資料量那條軸是平的**：120K → 180K 多 50% 資料只換到 +0.0067，CI 大幅重疊。
品質那條軸差距大得多（high_confidence 領先 0.03–0.04，CI 不重疊）。

⚠️ **兩個洩漏，數字只能參考**：
1. pseudo label 是舊 annotator 產生的，而舊 annotator **只讀得到第 3 張視圖**——
   這批資料等於在教模型模仿一個單視角模型，是循環的。
2. 產生 pseudo label 的 annotator 本身用專家標籤訓練，專家資訊會間接滲入。
   high_confidence 的優勢尤其可疑：「舊 annotator 最有信心的配對」本來就偏向和專家一致。

絕對數值（0.93–0.97）因此不能和守門 AUC（0.89–0.92）相比。

---

## 10. 關於「放大參數量」的討論

問題：參數量放大一個數量級，會不會像 LLM 那樣「完全記住訓練集的同時泛化更好」？

關鍵數字：

| | 數值 |
|---|---|
| 專家標註總量 | **1097 對**（訓練 932 / 驗證 165 / 測試 122） |
| 目前參數量 | 4,786,657 |
| 參數 : 獨立訓練樣本 | **5136 : 1** |
| 目前訓練集 acc / AUC | 0.95 / 0.993（best-val checkpoint） |

**我們早就在過參數化區間的深處**。雙下降的駝峰出現在插值門檻附近，
而 5136 個參數記一個樣本已經遠遠越過。訓練集 acc 沒到 100% 不是容量不夠，
是 checkpoint 取 best-val（epoch 80），到 epoch 300 訓練 loss 還會從 0.071 掉到 0.053。

**LLM 類比在這裡斷掉**：現代 LLM 不是「小資料 + 巨大模型」，而是「巨大資料 + 巨大模型」。
Chinchilla 大約每參數配 20 token；這裡是每樣本配 5136 參數，方向相反、差五個數量級。

現成的反例：concat 參數量 3 倍，legacy 下 AUC 完全沒動、val−train 落差從 0.0214 擴大到 0.0472。
但同樣 3 倍參數換成 physical augmentation 後跳到 0.9204——**容量不是沒用，是要有東西餵它**。

建議的驗證順序：
1. **資料縮放曲線**（25%/50%/100% 專家標註）——只用專家標註，不受 pseudo label 污染。
   還在陡升 = 資料受限，加參數必然無效。這是最該先做、最便宜的。
2. **寬度掃描**（0.5×/1×/2×/4×）——雙下降的正面測試。
3. 預訓練語料那條軸（§9 已做初步評估，結論是平的）。

**測量前提**：每折測試集只有 122 對，複製誤差 ±0.02。任何結論都要跨多折合併。

---

## 10b. 資料縮放曲線：**確認是資料受限**（2026-09-18）

只縮訓練集（val 165 對、test 122 對維持不變），依二值化標籤分層抽樣，
三折各跑完整曲線且整條留在同一節點。架構固定 3v + legacy aug。

| 訓練對數 | 測試 AUC（三折平均） | best val_loss |
|---|---|---|
| 233 (25%) | 0.8370 | 0.1316 |
| 466 (50%) | 0.8284 | 0.1186 |
| 699 (75%) | 0.8974 | 0.1095 |
| 932 (100%) | **0.9110** | **0.1037** |

逐折配對的增量（消掉折間變異）：

| 區間 | ΔAUC | Δval_loss |
|---|---|---|
| 233 → 466 | −0.0086（fold 2 異常 −0.064） | −0.0129 |
| 466 → 699 | +0.0690 | −0.0091 |
| 699 → 932 | **+0.0137**（三折皆正） | **−0.0058** |

**對 log2(n) 的斜率：AUC +0.0403 ± 0.0233、val_loss −0.0140 ± 0.0054（每倍增資料量）。**

### 結論

**曲線在 100% 處仍在上升，尚未飽和。** val_loss 的增量在遞減
（−0.0129 → −0.0091 → −0.0058，大約每次減半），但最後一段仍有改善；
AUC 的最後一段 +0.0137 三折同號。

**對照：每倍增資料量約 +0.04 AUC，而所有架構改動（§8a）都是 0.00–0.01 且全部不顯著。**
資料量的效應比架構大一個數量級。

⚠️ 用一個 octave 的 log-linear 擬合往外推有風險，且 AUC 斜率的折間變異很大（±0.023）。
但方向很明確：**要提升效能應該去擴專家標註，不是繼續改架構。**

## 11. 新增的檔案與旗標

| 檔案 | 用途 |
|---|---|
| `view_geometry.py` | 數值反解 3D 變換 ↔ 三視圖對應表，import 時自我驗證 |
| `train_annotator_3view.py` | 修正切片的 annotator 重訓入口（含各種對照組態） |
| `train_annotator_sweep.py` | 超參數掃描，權重寫到 `Annotator_Model_sweep/` |
| `tools/compare_annotator3v.py` | 新舊 annotator 配對 bootstrap 比較 |
| `tools/view_ablation.py` | 逐視角清零消融 |
| `tools/analyze_2x2.py` | 2×2 + lr 掃描彙整，含增益來源分解 |
| `tools/eval_pretrain_scaling.py` | 預訓練語料的資料量評估 |

新增的設定旗標（預設值都維持舊行為）：

- `Config.model_builder`（預設 `"MVCNN_Siamese"`，即**有 bug 的那個**）
- `Config.pool_type`（`"max"` / `"concat"`）
- `Config.aug_mode`（`"legacy"` / `"physical"`）
- `Config.model_kwargs`（傳給建模函數，如 `(("share_branch_norm", True),)`）
- `MVCNN_Siamese_3View(share_branch_norm=..., sym_merge=..., trunk_norm=...)`

⚠️ **地雷**：`Data_process_Train.py` 目前 uncomment 的設定區塊是 FineTune 那組，
所以直接 `python3 Data_process_Train.py` 用的仍然是有 bug 的 `MVCNN_Siamese`。
這是為了相容舊權重，但預設值指向已知錯誤的實作遲早會有人踩到。
建議把預設翻成 `MVCNN_Siamese_3View`，在 PreTrain / FineTune 設定裡明確寫上舊的。

---

## 12. 進度與待辦（2026-09-19 更新）

### 已完成（原本列為待辦）

- ✅ fold 1、2 的 2×2 補測 → §8a。**推翻了 §4 的結論**，三折合併為 null。
- ✅ `Annotator3vSharedBN` fold 0 → §8。對稱性與 AUC 皆 null，證實主因在 head。
- ✅ 資料縮放曲線 → §10b。**確認資料受限**，每倍增資料量約 +0.04 AUC。
- ✅ `Annotator3vSymMerge` fold 0 → §8。對稱性降到 1/28（AUC 仍 null）。

### 仍未做

| 項目 | 現況 | 優先度 |
|---|---|---|
| ~~`aug_mode="sign"`（8 個符號翻轉）~~ | ⛔ **作廢**。其動機（特徵向量符號歧義）建立在 §7 的錯誤前提上——視圖是標準腦座標，不是主軸座標，見 §0a。實際跑過的那一版還中了 §0b 的標籤洩漏 | 不做 |
| 寬度掃描（雙下降測試） | §10 的選項 (b)，未做。§10b 已顯示資料受限，這條線的預期報酬更低了 | 低 |
| **新預設下的對照基線** | `share_branch_norm` 預設已於 2026-09-18 改為 `True`，**目前所有結果都是舊架構訓練的**。未來任何比較都要先在新預設下重跑基線，不能跨架構比（§5 的教訓） | **高，只要還要做實驗** |
| SymMerge + 共用 BN 的精確對稱版 | 已寫進 `data_process_train_v2.py`（`share_view_norm=True` + `sym_merge=True`）。2026-09-19 14:08 起跑 10 折，這是**第一次**在沒有標籤洩漏的情況下訓練這個組態 | 進行中 |
| PreTrain / FineTune 用修正後架構重訓 | 未做 | 中 |
| `Data_process_Train.Config.model_builder` 的預設 | 仍是**有 bug 的** `MVCNN_Siamese`。直接跑 `python3 Data_process_Train.py` 會訓練到錯的模型（§11 的地雷）。建議翻轉預設並在舊流程明確指定，待決定 | 中 |
| val_auc vs val_loss 選擇準則 | 現在每次訓練自動存兩份 checkpoint，零額外成本。目前只有 1 折資料（差 −0.0024，null），需累積更多折才有結論 | 自動累積中 |
| 重測「90° 旋轉是否有用」 | §0a 記的 −0.1154 被 §0b 的洩漏污染，失效。要在修正後的管線上重跑才有意義 | 中 |

### 標準警告

- ⛔ **新 annotator 尚未驗證優於舊的，不要把線上服務切到新權重。**
  服務目前載入 `FineTune_Model/FineTune_miniLR_D1-D6_0.weights.h5`（`nrn_service/config.py:63`）。
- ⛔ 舊 annotator 權重（`Annotator_Model/Annotator_D1-D6_*`）在 2026-09-19 清理後**只剩這一份**，
  備份目錄已刪（已驗證與原檔逐位元相同）。要重訓 annotator 請用新檔名，不要覆寫。

### 根據目前證據，下一步該做什麼

§8a 顯示所有架構改動都是 null，§10b 顯示每倍增資料量約 +0.04 AUC——
**資料的效應比架構大一個數量級**。所以下一步應該是**擴充專家標註**，
而不是繼續在架構上找。上表裡的架構類待辦（符號翻轉、寬度掃描）在標註量顯著增加之前，
預期報酬都很低。
