# 提升全庫檢索表現：分階段計畫

**目前狀態**：階段 0 未做、**階段 1 已做（no-go）**、**階段 2 已做（go）**、階段 3 待做。

機制與實驗結果見 `analysis_model_results/FINDINGS.md` §10，
資料與腳本在 `analysis_external_validation/` 與 `analysis_hubness_debias/`。

**基線**（winnable 子集，rank-1 同型率 / 池內 AUC）：

| 模型 | rank-1 | 池內 AUC |
|---|---|---|
| `Annotator_D1-D6_0` | 31.0 % | 0.898 |
| `FineTune_miniLR_D1-D6_0` | 22.0 % | 0.861 |

---

## 0. 機制：三輪檢驗的結論

| 輪次 | 假設 | 結果 |
|---|---|---|
| 1 | 全域分數膨脹 | **否證**——池內 rank-1 對任何單調變換不變，全域平移不可能改變排名 |
| 2 | per-EM 偏移（海綿效應） | **實驗否證**（階段 1） |
| 3 | 分類頭的訓練目標錯配 | **成立**（階段 2） |

唯一不受 top-5 條件汙染的量測是**池內 AUC**（每顆 FC 各自算「型別相符 vs 確定不同型」
的 AUC 再平均）。annotator 在池的**各個深度**都較好（p@5 67.1 % vs 53.9 %、
正解 rank 中位 3 vs 5），所以「進場」與「排序」不是可分的兩件事，只有一個排序。

⚠️ 舊版曾據「top-5 內成對正確率 0.476 vs 0.456」主張 fine-tune 頂端排序較好，
**該主張已撤回**：兩數字算在幾乎不相交的樣本上（兩模型都評分的三元組僅 **23 組**），
cluster bootstrap 後 CI 大幅重疊。

---

## 階段 0：免費的改動（未做，先做、單獨做）

`confident_*` 目前取兩模型 top5 的**聯集**，實測反而稀釋品質：

| 候選集合 | 涵蓋 FC | 同型率 |
|---|---|---|
| annotator top5 單獨 | 1 276 | **32.5 %** |
| 聯集（現行） | 1 276 | 27.8 % |
| 交集 | 544（−57 %） | 33.9 % |

**改成單用 annotator 排序**，不需重訓。改 `tools/build_confident_list.py`。
交集只多 1.4 pp 卻掉 57 % 覆蓋率，不划算。

---

## 階段 1：per-EM 去偏 ✅ 已做 —— **no-go**

`analysis_hubness_debias/{dump_pool_scores,debias}.py`，結果見該資料夾 README。

| 模型 | 基線 rank-1 | 最佳去偏（center α=0.25） | 顯著性 |
|---|---|---|---|
| fine-tune | 21.97 % | 24.58 % | p = 0.055 |
| annotator | 31.01 % | 31.28 % | p = 0.791 |

11 種設定（置中、CSLS、z-score、池內 rank 正規化）× 2 模型 × 2 種基線估計集，
**沒有任何一組讓池內 AUC 上升**。強去偏災難性（z-score 讓 rank-1 掉到 0.8 %）。

→ 分數裡的 per-EM 成分**本身帶有真實訊號**，拿掉它破壞的比修好的多。
**不要為此增加一層後處理。**

（附帶：CSLS 原式 `2s − r_e − r_f` 的 `r_f` 在單一 FC 池內是常數，對 rank-1 完全無效，
等價於 `s − r_e/2`。它只在跨 FC 比較時有意義（門檻、confident list 排序），
不該當成獨立方法。另也否證了「EM 三視圖尺寸」假設，|rho| < 0.2 且方向矛盾。）

---

## 階段 2：凍結 trunk 探針 ✅ 已做 —— **go**

`analysis_hubness_debias/{probe_frozen_head,check_em_leakage}.py`。

⚠️ **「只重訓 head」的參數分布是反過來的：**

| 部分 | 參數量 | 佔比 |
|---|---|---|
| trunk（conv1-4 + 6 個 BN） | 66 528 | **1.4 %** |
| head（Dense 18432→256 + BN + Dense 256→1） | 4 720 129 | **98.6 %** |

探針凍結 1.4 %、重訓 98.6 %。結論應說成「**卷積特徵抽取器夠用，不必重訓；
問題出在它上面那個佔絕大多數參數的分類頭是為錯的目標訓練的**」。

held-out 638 顆 FC：

| head | rank-1 | 正解 rank 中位 | 池內 AUC |
|---|---|---|---|
| 原 head（基線） | 31.0 % | 3 | 0.8995 |
| 重訓（從現有權重，lr 1e-4） | **64.0 %** | **1** | **0.9606** |
| 重訓（從頭，lr 1e-3） | 52–63 % | 1–2 | 0.947–0.948 |

**不是 EM 側身分捷徑。** 把訓練時當過正例的 EM **全部移出候選池**後重排
（214 顆 FC、池中位仍有 1 816 個候選）：Δ **+17.1 pp**，CI [+9.1, +25.8]
（從頭版 +9.6 pp，CI [+0.5, +18.2]）。記住的正例已不在池裡。

⚠️ **限制**：
- **任務對齊的循環性未排除**。探針用型別標籤訓練也用型別標籤評估；
  原 head 是用專家**個體層級**標籤訓練的。不能讀成「個體層級會提升 33 pp」。
- **rank-1 執行間變異約 ±10 pp**（從頭版兩次 52.2 % / 63.2 %，池內 AUC 卻穩定）。
  **池內 AUC 才是穩健指標。**

**實作陷阱**：head 的 `BatchNormalization` 用預設 `momentum=0.99` 時，在數千步的規模下
moving 統計量嚴重滯後，推論時池內 AUC 掉到 **0.33**（低於隨機、系統性反向）。
必須降到 0.9 並提高每 epoch 步數（以「錨點 =（FC, 正例）」迭代，而非以 FC 迭代）。

---

## 階段 3：正式訓練 ✅ 已做（fold 0）—— 結果見 §階段 3 結果

探針只是診斷，**不是可用的模型**。差距見 §3.0。

### 3.0 探針與正式模型的差距

| 項目 | 探針 | 正式模型需要 |
|---|---|---|
| 訓練資料 | 638 顆 FC、4 643 個錨點 | 全部可用 FC |
| 增強 | 無 | 旋轉／翻轉／FC-EM swap |
| 損失 | 純 InfoNCE | **加上專家二元項**（錨定絕對校準） |
| 輸出 | logit，無可用門檻 | 可用的 0.5 門檻 |
| 評估 | 只有型別層級檢索 | 加上十折測試集（二分類不得退步） |
| trunk | 凍結 | 可繼續凍結（見下） |

### 3.1 成本比原估計低很多

階段 2 證明 **trunk（66 K 參數）可以維持凍結**，只需訓練 head（4.7 M）。這表示：

- 不需要處理 trunk 裡那 6 個不共享的 BN（`model.py:509`）的兩次 forward 問題
- 特徵可以對「抽樣到的配對」預先算好並快取，訓練變成純 MLP，極快
- 原估計的「1–2 天」可降到**半天到一天**

⚠️ 但特徵**不能 per-neuron 快取**：前處理是成對相依的
（`swc_util._pad_to_same_size` 以「這一對裡較大的那張」為準補零，
`nrn_service/scoring.py:8-10` 有記載），同一顆 EM 配不同 FC 時像素不同。

### 3.2 三元組：從整個候選池挖

`pair_labels.csv` 是兩模型 top-5 的聯集，只從那裡取三元組等於只教模型重排 5 個東西：

| | 每顆 FC 的負例數 |
|---|---|
| top-5（不可用） | 中位 **4** |
| 整個候選池 | 中位 **2 770** |

`analysis_hubness_debias/scores/pool_scores_*.parquet` 已經有全池分數
（各 5 308 333 對），`debias.py` 的 `label_pairs()` 可直接判定每一對。

**損失用 InfoNCE**（1 正例 + K 個池內負例，K = 32–64），直接優化 rank-1 / MRR。

**正例也要從池裡挖。** 池內每顆 FC 的同型 EM 中位有 111 顆，與負例來自同一次 dump、免費。
這是取捨不是筆誤：

- 池內正例 → 錨點多兩個數量級，直接優化型別層級檢索；
- 但隨機一顆同型 EM 是**弱正例**（KC 有 138 個等價候選），會把模型更推向型別層級、
  離個體層級更遠。

**決定：混合**——每個 anchor 取 1 個 top-5 正例 + 少量池內正例，
並在 §3.6 加一條守門（專家配對的個體層級 rank 不得退步）。

### 3.3 切分：對全部 1 276 顆 winnable FC 切

只切 804 顆的話 eval 半邊約 403 顆，annotator 在該子集本就有 35.4 %（全 winnable 30.3 %），
與 headline 不可比。**改成對全部 1 276 顆做 FC 層級分層切分**，
三元組只取 train 半邊，評估跑 eval 半邊（約 638 顆）。

⚠️ **口徑統一**：`31.0 %` 是可判定分母（排除 rank-1 落在未定型 EM 的情形），
`30.3 %` 是全體分母。全篇擇一並標註。

### 3.4 EM 側洩漏：必須分層回報

FC 層級切分後，**eval 半邊的 EM 有 63.0 % 出現在 train 半邊**；
負例 EM 高度集中——782 顆不同 EM 佔 3 628 筆負例，前 20 顆佔 31 %。

階段 2 用的檢查（`check_em_leakage.py`）必須沿用，尤其是：

- **移除所有訓練正例 EM 後重排**（最能區分記憶 vs 型別知識，階段 2 就是靠它定案）
- 「候選池限縮到全未見 EM」這項**統計力不足**（n 只剩 35，CI 跨零），
  可以報但不能當主要證據

### 3.5 程式改動

**(a) 分類頭拆成 logit + sigmoid** —— 已實測可行。
`Dense(1, activation="sigmoid")`（`model.py:557`）拆成 `Dense(1)` + `Activation("sigmoid")`，
`load_weights()` 舊檔成功且輸出**逐位元相同**（TF 2.19.1 / Keras 3.12.0，max abs diff = 0.0）。
**10 分鐘的改動。** 排序項用 logit（有趣區間在 0.88–0.99，sigmoid 梯度被壓扁），
BCE 項留 sigmoid。

**(b) 聯合損失 —— 一次訓練、每 step 兩個損失，不是兩個階段。**

```python
def train_step(self, data):
    batch_A, batch_B = data              # A = InfoNCE, B = 專家配對
    with tf.GradientTape() as tape:
        loss_rank = self.infonce(batch_A)      # logit
        loss_bce  = self.bce(batch_B)          # sigmoid，一般 BCE
        loss = self.w_rank * loss_rank + self.w_bce * loss_bce
    g = tape.gradient(loss, self.head.trainable_weights)   # trunk 凍結
    self.optimizer.apply_gradients(zip(g, self.head.trainable_weights))
```

**為什麼二元項不能拿掉**：排序損失只看分數差，對全域平移無感。模型可把分數壓進
`[0.30, 0.52]`，排序完全正確但 **0.5 門檻就廢了**。margin 逼出最小間距但**不錨定位置**。
另外專家標註是**個體層級**的（每顆 FC 中位數只給 1 個正例），比型別層級嚴格。

**為什麼不能分兩階段**：序列訓練會讓最後階段重新施加自己的校準，把前面的修正洗掉——
正是 §10 診斷的 annotator → pseudo → pretrain → finetune 的毛病。

**(c) focal loss 拿掉。** `BinaryFocalCrossentropy(gamma=2.0)`
（`data_process_fineTune.py:353`）**降低容易樣本的權重**，而這批負例是
「模型極度自信但錯」的，focal 會**放大**其梯度。二元項改用一般 BCE。

**(d) 增強**（若不凍結 trunk 才需要；凍結時特徵已固定）：
沿用 `2 swap × (4 rot + 1 flip) = 10` 種。FC/EM swap 照做但兩分支同步交換——
兩側 BN 不共享、架構上不對稱，對稱性是靠 swap 學出來的。旋轉／翻轉對所有圖施加同一變換。

**(e) epoch 定義。** 專家 10 970 vs 三元組數量不等，較小的要 `.repeat()`，
用 `steps_per_epoch` 以三元組數量定義。

### 3.6 評估與守門

| 指標 | 用途 |
|---|---|
| **MRR、正解 rank 中位** | **主指標**（見下方更正） |
| 池內 AUC | 輔助（ALPN 曾出現 AUC 0.997 但 rank-1 5.8 %，與檢索指標可反向） |
| rank-1、MRR、p@5、正解 rank 中位 | 輔助 |
| 專家測試集 AUC | 守門：與**同一折**的 annotator 做配對 bootstrap（⚠️ 原寫「不得低於 0.9293」有誤，見 §階段 3 結果的更正） |
| 專家配對的個體層級 rank | 守門：不得退步（防止被型別層級弱正例拉偏） |
| 移除訓練正例 EM 後的 Δ | 守門：確認不是身分捷徑 |

⚠️ **更正**：原本把池內 AUC 列為唯一主指標是錯的。ALPN 的池內 AUC 可達 0.997 而 rank-1 只有 5.8 %，
兩者會往相反方向走。主指標已改為 **MRR + 正解 rank 中位**，rank-1 一律附 cluster bootstrap CI。

**評估成本**：eval FC 的整個池約 638 × 2 900 ≈ 185 萬對。trunk 凍結時可快取特徵，
但因成對相依仍需逐對前處理（約 0.38 ms/對 → 約 12 分鐘）。
→ checkpoint 用便宜的代理指標（held-out 三元組正確率）挑，少數幾個才跑完整檢索評估。

`run_type_check.py` 需參數化：`--fc-subset`，且 `SCANS` 目前寫死，要能指向新模型的掃描。

### 3.7 從哪個模型出發

**`Annotator_D1-D6_0`** 的 trunk（檢索 31.0 % vs 22.0 %，階段 2 的探針也是在它上面做的）。
⚠️ 它只是十折之一，沒看過 fold 0 的標註；產品模型需用全部標註重訓。

### 3.8 族群配額

- **go/no-go 放在 KC**（缺口完全在 KC：18.1 % vs 32.2 %，p = 1.6e-12）。
- **LC-only 只是管線 smoke test**，不是假設驗證——LC 本就是兩模型打平的族群，
  「沒變化」在兩種情況下都是預期結果。
- **ALPN 排除**，不給配額。69 顆太少，池中位 3 109 個候選配 1 個正解，兩模型都約 3 %。

---

## 階段 3 結果：fold 0 —— **no-go**（三個 bug 已修，但型別泛化失敗）

> ⛔ **2026-09-18：本節所有實驗的 trunk 都只讀到三視圖中的第 3 張。**
> `model.MVCNN_Siamese` 的視角切片有閉包延遲綁定錯誤，annotator 本身就是單視角訓練的，
> 排序微調沿用它的 trunk。下面的結論是「單視角模型之間」的比較，
> 修正後（`MVCNN_Siamese_3View`）是否仍成立要重測。見 `MODEL_PIPELINE_HANDOVER.md` §13.0。

> ### ⏸️ 本節所有數字待重測（2026-09-15 發現）
>
> 六個組態全部 `augment=True`（預設），而增強是**逐對**施加的：
> 一個錨點的正例與它的 32 個負例各自抽到不同的 `(rot, flip)`，
> 然後被直接拿來比大小（`data_process_rank.py:734,736`）。
> 探針完全沒有增強，所以沒踩到這個問題。
>
> **實測**（400 顆 FC × 20 次模擬，用訓練自己的負例池）：
>
> | | 值 |
> |---|---|
> | 負例之間的 logit 標準差 | 0.974 |
> | 同一對在 8 種 rot/flip 下的 logit 標準差 | 0.449 |
> | 最大 vs 次大負例的差距（中位） | **0.265** |
> | **argmax 被朝向噪音換掉的比例** | **45.9 %** |
> | max 被噪音抬高的量 | +0.225（3.2 個 τ） |
>
> 抬高量只有 3.2 個 τ（e³·² ≈ 25 倍），**不足以讓損失被噪音主導**——
> 負例之間真實的形態差異是噪音的 2.17 倍。
> 但最難與次難的負例差距（0.265）小於噪音（0.449），所以
> **有 45.9 % 的時候「哪個負例最難」是朝向運氣決定的**。
> 梯度有將近一半的時間在追「剛好轉到好角度的那個負例」。
>
> ⚠️ 逐樣本增強配 InfoNCE 本身是標準做法（SimCLR/MoCo）。
> 這裡出問題是因為 **trunk 凍結、從未為旋轉不變性訓練過**，
> σ = 0.449 是不可約的噪音地板；SimCLR 那裡編碼器會學到不變性。
> 可檢驗的預測：`--tune-trunk` 與完全從頭那兩輪應受害較輕。
>
> **修法**：`(rot, flip)` 改成**每個錨點群組抽一次**（正例 + 它的 K 個負例共用），
> 錨點之間仍各自不同。BN 多樣性不受影響（一個 batch 仍有 128 種朝向），
> 也不會退回「每 epoch 共用」那個舊 bug。專家 BCE 那批維持逐對（無跨樣本比較）。
>
> **受影響的結論**：
>
> | 結論 | 狀態 |
> |---|---|
> | 「專家 BCE 項不能拿掉」 | ❌ 未證實——它是在排序項壞掉的前提下得到的 |
> | 「訓練更久沒有用」（e35 < e10） | ❌ 未證實——與「在擬合朝向噪音」一致 |
> | 「機制假設：型別標籤無法懲罰通用好度」 | ❌ 檢驗它的實驗是壞的，既未支持也未否證 |
> | 六個組態的守門與檢索數值 | ⏸️ 待重測 |
> | 「六個組態皆未通過守門」 | ✅ 對**已執行的這些跑法**成立；修好後是否仍然如此未知 |
> | EM 先驗劑量反應 | ⚠️ 三個**訓練過**的組態共享同樣污染、彼此可比；但基線（3.9 %）沒訓練過，「基線 → 完全從頭」那一步被混淆 |
> | `expert_em_prior.py`（富集 0.2x） | ✅ **不受影響**——純分析，不涉訓練與增強 |
>
> ### ⭐ 回歸測試的結果（2026-09-16）：找到兩個 bug，落差全部解釋
>
> 把 `data_process_rank.py` 設成探針組態（`--seed 7 --no-augment --w-bce 0
> --n-pos 8 --k-within 32 --k-cross 0 --neg-pool 64 --train-ratio 0.5
> --dev-ratio 0 --epochs 60 --pick-epoch 60`），基線逐項重現探針
> （rank-1 31.04 / MRR 0.4585 / median 3 / p@5 65.67，四項一字不差），
> 確認評估路徑與 FC 集合同一。然後逐項修：
>
> | | rank-1 | MRR | median | p@5 | KC r1 |
> |---|---|---|---|---|---|
> | 基線 | 31.04 | 0.4585 | 3 | 65.67 | 32.15 |
> | 對齊後（未修） | 32.59 | 0.4889 | 2 | 71.79 | 41.30 |
> | + 修專家 forward 的 BN 副作用 | 53.46 | 0.6048 | 1 | 66.77 | 68.10 |
> | + head BN 換 LayerNorm | **66.72** | **0.7433** | **1** | **86.21** | **71.39** |
> | 探針（標的） | 63.20 | 0.6772 | 1 | 77.74 | — |
>
> **兩個 bug 解釋了全部 30.6pp 的落差，修完後正式版勝過探針。**
>
> **Bug 1：`--w-bce 0` 沒有真的關掉 BCE 項。**
> 它只讓梯度歸零，`head(fe, training=True)` 這次 forward 照樣更新 head BN 的
> moving 統計。三次 forward（正例 128 / 負例 4096 / 專家 128）各自更新，
> 穩態權重 29.9 % / 33.2 % / **36.9 %**（momentum 0.9，與批次大小無關）。
> 檢索評估只打 pool 配對，卻有 36.9 % 的正規化統計來自專家分布。
> ⚠️ 這**不只影響 `--w-bce 0`**：w_bce > 0 的組態同樣三分布混合，
> 而且守門只打專家配對、BN 卻有 63.1 % 來自 pool——兩個指標各自都錯配。
>
> **Bug 2（更根本）：同一個比較內部的正規化不一致。**
> `sp_` 用 128 個正例的批次統計、`sn_` 用 4096 個負例的批次統計，
> 然後 `tf.concat` 起來比大小——與朝向 bug 同一類錯誤。
> BN 會減掉批次均值，而「正例整體分數較高」正是被比較的訊號本身。
> **這一條探針也有，所以探針的 63.20 % 同樣不是乾淨數字。**
>
> **事後補救無效（已實測）。** `--load-head ... --recalib 50 --recalib-src {pool,expert}`
> 把 moving 統計重新收斂到單一分布後重評 headline checkpoint：
>
> | scratch e10 | rank-1 | MRR | p@5 | 守門 AUC |
> |---|---|---|---|---|
> | 原始（混合 BN） | 34.62 | 0.489 | 70.23 | 0.8122 |
> | 重校到 pool | 17.43 | 0.310 | 46.21 | 0.7928 |
> | 重校到 expert | 14.67 | 0.249 | 34.46 | 0.7815 |
>
> 兩個方向都更差——因為訓練時根本不存在單一正規化可供重現。
> → **六個組態的數字救不回來，只能重訓。**
>
> **修法：head 的 BN 改成 `LayerNormalization`。**
> 根本理由不是「被咬三次」，而是：用於檢索的打分函數，對同一對神經的輸出
> 不該取決於「同批次裡還有誰」。LN 逐樣本正規化，訓練與推論行為完全相同，
> 這一整類 bug 從函數定義上消失。head 只是兩層 MLP，沒有代價。
> `--head-init existing` 的相容性：BN 的 4 個權重只搬 gamma/beta 到 LN。
>
> ⚠️ **副作用**：LN 移除了 BN 批次噪音的隱性正則，訓練損失在 e20 就近乎歸零
> （BN 版要到 e30–40）。本輪取 e60 的權重評估仍是最佳，沒有實際受害，
> 但之後的組態要留意過擬合。
>
> ⚠️ **新的最大問題：`em_prior` 衝到 45.57 %**（基線 7.67 %），LC 更達 55.41 %。
> 一個完全忽略 FC、只按每顆 EM 全域平均分排序的虛無模型就能拿 45.57 % rank-1。
> RankTune 的 66.72 % 高過它，但不足以宣稱增益主要來自形態判別。
> 探針的表沒有這一欄，無從對照。**這是目前最該追的問題。**
>
> ### ⭐⭐ EM 型別 hold-out（2026-09-16）：增益不能外推到訓練時缺席的型別
>
> `--holdout-emtypes "LC12,KCab-s,DL2d_adPN"` 讓這些 np_type 的 EM
> **完全不進訓練**（不當正例也不當負例）。正例全落在被 hold out 型別裡的 FC
> 會因為沒有正例而自動退出訓練，它們正是乾淨的測試層。
> 組態：探針對齊 + LN + `--fixed-anchors`（目前最好的組合）。
>
> | 層 | n_fc | | MRR | median_rank | rank-1 | p@5 |
> |---|---|---|---|---|---|---|
> | holdout:LC12 | 54 | 基線 | 0.3358 | 8 | 25.00 | 46.30 |
> | | | **RankTune** | **0.0461** | **24** | **0.00** | **0.00** |
> | holdout:KCab-s | 85 | 基線 | 0.4962 | 3 | 29.41 | 76.47 |
> | | | **RankTune** | **0.1563** | **11** | **0.00** | **36.47** |
> | holdout:DL2d_adPN | 34 | 基線 | 0.1822 | 6 | 0.00 | 41.18 |
> | | | RankTune | 0.2176 | 5 | 0.00 | 52.94 |
> | （對照）KC 整體 | 423 | 基線 | 0.4909 | 3 | 32.15 | 72.10 |
> | | | RankTune | 0.7637 | 1 | 71.63 | 82.51 |
>
> **同一個模型在沒被 hold out 的資料上大幅勝過基線（KC 32.15 → 71.63），
> 在訓練時缺席的型別上卻遠低於未訓練的基線。** LC12 的 p@5 是 0.00——
> 正解連前五名都進不去，中位排名從 8 退到 24。
>
> 唯一例外是 **DL2d_adPN**（正解只有 1 顆 EM 的 ALPN）：MRR 0.1822 → 0.2176、
> p@5 41.18 → 52.94，小幅但真實的增益。那正好是先驗最無用武之地的一層。
>
> **機制不是「壓低沒見過的 EM」。** 實測分數分布相反：被 hold out 的正解 EM
> 平均分 3.2606，見過的正解 3.2337，幾乎相同。模型沒有壓低它們，
> 而是**對它們的鑑別力不足**——正解拿了高分，但同時有二十幾顆別的 EM 更高。
>
> ⚠️ **結構上無法分離的混淆**：被 hold out 的 FC 其正例就是那些 EM，
> 所以 FC 型別與 EM 型別必然一起被拿掉，無法只 hold out 其中一個。
> 因此本實驗證明的是「**無法泛化到訓練時缺席的型別**」，
> 而不是更強的「只記住 EM body」。
> 對一個要掃全庫的工具，這仍然是決定性的：多數 FC 的型別在訓練集裡樣本極少。
>
> ### ⭐⭐⭐ 控制組與生產組態：兩種獨立的失效
>
> **控制組**（`--report-emtypes`，同一批 FC 但模型訓練時**看過**那些型別，
> 其餘設定與 hold-out 那輪完全相同、訓練逐位元一致）：
>
> | 層 | 基線 MRR | 見過（控制組） | 沒見過（hold-out） |
> |---|---|---|---|
> | LC12 (54) | 0.3358 | **0.9907**（rank-1 98.15 / p@5 100） | **0.0461**（0 / 0） |
> | DL2d_adPN (34) | 0.1822 | **0.9706**（94.12 / 100） | 0.2176（0 / 52.94） |
> | KCab-s (85) | 0.4962 | **0.1324**（0 / 15.29） | 0.1563（0 / 36.47） |
>
> **失效一：型別缺席 → 崩潰。** LC12 98.15 % → 0.00 %、DL2d_adPN 94.12 % → 0.00 %。
>
> **失效二：少數姊妹型別 → 即使見過也崩潰。** KCab-s 在**見過**的情況下同樣是
> 0.00 %（MRR 0.1324），比 hold-out 版還差，遠低於基線的 0.4962。
> 最可能是多數壓制：KCab-c 有 376 顆 FC、KCab-s 只有 180 顆，兩者形態相近，
> 訓練權重壓倒性地投給 KCab-c，KCab-s 的 FC 全被搶走。
> **這一種先前完全沒看到**，因為族群報表把它藏在 KC 整體的 73.05 % 底下。
>
> ⚠️ 控制組那些漂亮數字的成色，`em_prior_p@5` 一欄就戳破了：
> **LC12 控制組的先驗 p@5 是 96.00 %**。一個完全不看 FC 的排名表在 LC12 上
> 就能拿 96 % 的 p@5，所以 98.15 % 的 rank-1 基本上是先驗水準。
>
> #### 生產組態（`w_bce=10` + cross 負例 + 修好的增強 + LN + 固定錨點，383 顆 eval）
>
> | 層 | 基線 MRR | RankTune | rank-1 | p@5 | median |
> |---|---|---|---|---|---|
> | 全部 (383) | 0.4638 | **0.6258** | 31.72 → 55.53 | 66.84 → 68.15 | 3 → 1 |
> | holdout:LC12 (32) | 0.3441 | **0.1678** | 25.93 → 3.33 | 56.25 → 25.00 | 5 → 10 |
> | holdout:KCab-s (57) | 0.5097 | **0.1009** | 33.33 → 3.51 | 73.68 → **7.02** | 3 → **21** |
> | holdout:DL2d_adPN (27) | 0.1898 | **0.1460** | 0 → 0 | 44.44 → 25.93 | 6 → 7 |
>
> 守門 0.9050 → **0.8585**（配對 bootstrap 差距 +0.047 [−0.008, +0.105]，**不顯著**；0.9293 是十折門檻，不適用於單折，見下方更正）。
>
> **先驗是可控的**：生產組態的 em_prior 只有 **8.90 %**（基線 3.87 %），
> 對照探針組態的 44.51 %。cross 負例與專家 BCE 確實在做它們該做的事，
> 六個組態的設計方向沒錯。
>
> **但型別泛化沒有解決，而且與先驗無關**——先驗壓到 8.90 % 之後，
> hold-out 層仍然低於未訓練的基線。
>
> ⚠️ **加上 CI 之後，三層的證據強度並不相同**（n 只有 27–85，CI 很寬）：
>
> | 未見型別 | 生產組態（383 eval） | 探針組態（638 eval） |
> |---|---|---|
> | **KCab-s** | 0.510 [.413,.603] vs **0.101** [.061,.153]：**不重疊** | 0.496 [.425,.573] vs **0.156** [.129,.183]：**不重疊** |
> | LC12 | 0.344 [.201,.505] vs 0.168 [.105,.237]：略重疊 | 0.336 [.224,.458] vs **0.046** [.038,.053]：**不重疊** |
> | DL2d_adPN | 0.190 [.146,.239] vs 0.146 [.125,.169]：重疊 | 0.182 [.149,.216] vs 0.218 [.177,.261]：重疊 |
>
> - **KCab-s 是唯一在兩輪都明確更差的**（p@5 73.68 → 7.02、中位排名 3 → 21）。
>   而且它在**控制組**（見過該型別）同樣崩潰（MRR 0.1324），
>   所以它的失效是**多數姊妹型別壓制**，不是缺席。
> - **LC12 在樣本較大的那輪明確更差**（p@5 46.30 → 0.00），生產組態那輪 n=32 判不出來。
>   它是「缺席型別」這個問題的代表。
> - **DL2d_adPN 兩輪都無顯著差異**。它的正解只有 1 顆 EM，先驗使不上力，
>   模型既沒靠先驗得分也沒被破壞。
>
> → 已證實的損害是**兩個獨立問題**：少數姊妹型別被多數搶走（KCab-s），
>   與完全缺席的型別排序變差（LC12）。
>
> ⚠️ **dev 完全偵測不到這個失效**：這一輪的 dev 三元組正確率是 **0.855**，
> 一路平滑上升到最後。dev 的負例來自訓練見過的型別分布，
> 問的是模型已經會的那件事。**任何以 dev 挑 checkpoint 的流程都會選中一個
> 在缺席型別上崩潰的模型而毫無警訊。**
>
> ### ⚠️ 守門指標的兩個更正（2026-09-18，`analysis_hubness_debias/expert_type_consistency.py`）
>
> **1. 0.9293 這個門檻用錯了。** 它是 annotator **十折合併**的 AUC，
> 而 annotator 自己在 **fold 0 只有 0.9050**，同樣過不了。
> 拿 fold 0 的 RankTune 去比十折門檻是蘋果比橘子。
> 單折實驗的正確比法是：**同一個 122 對的測試集上，與 annotator 做配對 bootstrap。**
>
> **2. 修好 bug 之後，守門上的差距不顯著。**（fold 0 測試集只有 122 對：59 正 / 63 負）
>
> | 組態 | RankTune AUC | annotator − RankTune [95 % CI] | P(annotator 較好) |
> |---|---|---|---|
> | `w_bce=10` 生產組態（含 hold-out） | 0.858 | +0.047 [−0.008, +0.105] | 0.957 |
> | `w_bce=50`（含 hold-out） | 0.865 | +0.040 [−0.008, +0.096] | 0.948 |
> | `--head-init existing`（舊 bug 版） | 0.856 | +0.049 [−0.004, +0.106] | 0.964 |
> | scratch e10（舊 bug 版） | 0.812 | **+0.093 [+0.033, +0.156]** | 0.998 |
> | `--w-bce 0`（各輪） | 0.60–0.64 | **+0.27 ~ +0.31** | 1.000 |
>
> CI 都包含 0：**「個體層級比 annotator 差」這句話，對修好 bug 的組態不成立**，
> 只能說「方向偏差、樣本太小判不出來」。明確較差的只有舊 bug 版與完全沒有 BCE 的組態。
> （`trunkscratch` 那輪的 test_label 檔是修正前產生的，基線欄是隨機網路，不可用。）
>
> **3. 專家標註與型別判定幾乎沒有交集，衝突假設無從成立。**
> 型別判定只涵蓋 KC / LC / ALPN 三族；專家標註的 FC 多是 PPL1 多巴胺神經、
> MBON14、fruitless 神經等（D1/D2 是依 MB 投射挑的）。
> 訓練集 1 097 對只有 **30 對**套得上型別判定（FC 側缺 VFB 型別預期；EM 側全都有型別），
> 測試集 122 對只有 **6 對**；**同型（1.0）配對一對都沒有**。
> 在這點交集裡：型別 0.0 的 22 對專家分數全是 0（**零硬衝突**）；
> 型別 0.5 的 14 對裡 7 對專家判正例（0.5 = 同亞族無法斷定，不算衝突）。
> 所以排序項與專家 BCE **從來沒有碰到同一批配對**，不存在直接的標籤衝突。
> 守門若有差距，更可能是**族群轉移**：排序項把 head 往 KC/LC/ALPN 的型別分辨拉，
> 而守門量的是多巴胺神經、MBON 這些完全不同的族群。
>
> #### 權重掃描：`w_bce` 10 → 50（2026-09-16，其餘同生產組態、含 hold-out）
>
> | | annotator | w_bce=10 | w_bce=50 |
> |---|---|---|---|
> | 梯度比（排序/BCE，已含權重） | — | 約 0.5–1.8 | **0.3–0.6** |
> | 全體 MRR | 0.4638 | 0.6258 | 0.6205 |
> | 全體 rank-1 | 31.72 | 55.53 | 53.30 |
> | 守門 AUC | **0.9050** | 0.8585 | 0.8649 |
> | holdout:KCab-s MRR | **0.5097** | 0.1009 | 0.1102 |
> | holdout:LC12 MRR | **0.3441** | 0.1678 | 0.2021 |
> | holdout:DL2d_adPN MRR | **0.1898** | 0.1460 | 0.1264 |
>
> BCE 梯度變成排序項的 2–3 倍，**幾乎每個數字都沒動**：整體增益還在，
> 守門與 annotator 的差距仍不顯著（0.865，CI 含 0），hold-out 層仍遠低於 annotator。
> → **型別泛化的傷害與損失權重無關**，只要排序項存在就會出現。
> ⚠️ 先前寫「生產組態的專家項被排序項壓過」是錯的：`grad_norms` 印出的範數
> 已乘上權重，`w_bce=10` 時兩項大致平衡、早期甚至 BCE 較大。
> ⚠️ 限制：優化器是 AdamW（逐參數自適應步長），損失權重的槓桿本來就小；
> 本結論只涵蓋 AdamW 下 `w_bce` 10–50。`em_prior` 在 w_bce=50 升到 33.85 %，
> 但該指標受評估集同質性污染，不據以下結論。
>
> ### 📌 結論
>
> **這個方法學到的是型別專屬的關聯，不能外推。** 它用「見過的型別上接近滿分」
> 換來「缺席型別與少數姊妹型別上遠差於基線」。對一個要掃全庫的工具，
> 這個交換是虧的——多數 FC 的型別在訓練集裡樣本極少或完全沒有，
> 而基線（annotator）雖然平庸但在各層之間均勻得多
> （LC12 25.00 / KCab-s 33.33 / KC 32.68 / 全部 31.72）。
>
> **重跑六個組態沒有意義**：它們調的旋鈕（`head_init`、`w_bce`、`epochs`、
> `trunk_init`）沒有一個碰得到型別泛化。要處理的是訓練集的型別涵蓋率，
> 或是換一個不依賴型別關聯的方法。
>
> ### ⚠️ 兩個診斷指標的限制（都是這一輪才發現的）
>
> **1. `em_prior` 被評估集的同質性污染。** 先驗虛無模型對每顆 FC 幾乎都推同一顆 EM
> （實測探針分數：319 顆 FC 的 top-1 只用到 30 顆不同的 EM，其中一顆 KCab-m
> 獨佔 208 顆）。所以它的數值高低取決於「那顆居首的 EM，其型別在評估集裡多常見」。
> KC 佔評估集 66 % 且型別層級高度同質，一顆 KC 型別的 EM 就能對上半數 KC FC——
> 這就是為什麼 KC 先驗在 probe3/4/5 三輪都固定是 **49.52 %**。
> 那不是模型性質，是評估集的結構。
> **`em_prior` 混淆了「模型 hubness」與「評估集同質性」，絕對值不可直接解讀。**
>
> **2. per-EM 置中會給出虛假的安心。** 置中只移除「與 FC 無關的常數偏置」，
> 移除不了「這顆 FC 對應到那顆特定 EM」這種 FC-conditional 的關聯。
> 實測 hold-out 那輪的模型：置中後 rank-1 仍是基線的 2.6 倍
> （33.12 vs 12.54、MRR 0.4494 vs 0.2750），看起來很紮實，
> 但同一個模型在 hold-out 層上是 0.00。
> **置中不是泛化測試，hold-out 才是。**
>
> ### ⭐ 固定錨點（`--fixed-anchors`）
>
> 探針的正例是抽一次就固定，正式版原本每 epoch 重抽。改成固定後：
> rank-1 66.72 → **73.35**、ALPN 8.70 → **50.72**（MRR 0.3142 → 0.6406）。
> ⚠️ 但**先驗幾乎沒動**（45.57 → 44.51），所以「重抽正例造成先驗」這個假設**否證**。
>
> ### 還有第二個差異：負例池的大小
>
> 探針每顆 FC **預先固定抽 64 個負例**，每步從這 64 個裡抽 32
> （`probe_frozen_head.py:49-50, 235`）；正式版每步從整個型別不符池
> （中位數千個）重抽 24。這是難度完全不同的兩個任務，**與增強無關**。
> 所以「重現不出探針的約 63 %」有兩個獨立嫌疑。
>
> ### 重跑順序
>
> 1. 改掉增強的施加層級（一行）。
> 2. **回歸測試**：把 `data_process_rank.py` 設成探針組態
>    （`--w-bce 0`、無 cross 負例、`n_pos=8`、不 early-stop、60 epoch，
>    **且負例池固定成 64 個**），看能不能重現探針的約 63 %。
>    重現得出來才代表兩支程式對齊，後面的消融才有意義；
>    重現不出來，階段 2 那個「go」本身要重新檢視。
> 3. 對齊後逐項放開（先放負例池，再放 cross 負例、BCE），定位差異來源。
> 4. 重跑六個組態。
> 5. 再做 `n_correct_in_pool` 分層的機制驗證。


實作在 `data_process_rank.py`（架構與參數見 `MODEL_PIPELINE_HANDOVER.md` §14）。
以下全部是 **fold 0、trunk 凍結、`--head-init scratch`**，評估集 383 顆 FC。

⚠️ **檢索指標用的是型別層級的弱標籤，不是專家標註。** `rank-1` 要讀成
「排第一的 EM **型別對**」，不是「就是那一顆」——KC 的池裡中位有 111 顆同型 EM。
專家標註只進入 BCE 訓練項與守門指標（專家測試集 AUC），後者才是個體層級。
兩個評估集幾乎不重疊（2 735 顆 eval FC 中只有 6 顆曾被專家標註過）。

### 結果（e10 = dev 最佳 checkpoint）

| family | 指標 | 基線（原 head） | RankTune |
|---|---|---|---|
| 全部 (383) | MRR | 0.464 | 0.489 |
| | rank-1 | 31.7 [26.9, 36.6] | 34.6 [29.7, 39.8] |
| | em_prior | 3.9 % | 11.4 % |
| KC (254) | MRR | 0.496 [0.450, 0.540] | **0.578 [0.532, 0.624]** |
| | rank-1 | 32.7 [26.8, 38.6] | **40.6 [34.7, 46.5]** |
| | em_prior | 0.0 % | 5.7 % |
| LC (88) | rank-1 | **45.5 [35.1, 55.8]** | 29.0 [18.8, 40.6] |
| | em_prior | 18.9 % | 35.0 % |
| ALPN (41) | rank-1 | 0.0 | 7.3 [0.0, 17.1] |
| 守門 | 專家測試 AUC | **0.9050** | 0.8122 |

CI 為 cluster bootstrap by FC、2 000 次重抽。

- **KC 有真實增益**，兩組 CI 幾乎不重疊——這正是 §3.8 指定的 go/no-go 族群。
- **LC 明確退步**，且先驗同步上升（18.9 → 35.0 %）。LC 本就是基線最強、
  而且基線本身已帶先驗成分的族群。
- **整體 CI 大幅重疊**，沒有可宣稱的整體增益。
- **守門沒過**（0.9050 → 0.8122，門檻 0.9293）。

**結論：不進生產、不跑全折。**

### 五個消融（同切分、同評估集，訓練為確定性，逐 epoch 損失可逐字重現）

| 組態 | 全部 MRR | rank-1 | p@5 | em_prior | KC rank-1 | LC rank-1 | 守門 AUC |
|---|---|---|---|---|---|---|---|
| 基線 | 0.464 | 31.7 | **66.8** | 3.9 % | 32.7 | **45.5** | **0.9050** |
| scratch e10（dev 最佳） | **0.489** | 34.6 | **70.2** | 11.4 % | **40.6** | 29.0 | 0.8122 |
| scratch e35（`--pick-epoch 35`） | 0.389 | 22.5 | 61.4 | 3.8 % | 28.7 | 10.0 | 0.8138 |
| `--w-bce 0` | 0.310 | 14.9 | 53.0 | 0.0 % | 18.5 | 10.5 | 0.5311 |
| `--head-init existing` e20 | 0.458 | **34.8** | 59.3 | **20.9 %** | 36.6 | **43.3** | **0.8561** |
| `--trunk-init scratch` e30（完全從頭） | 0.350 | 24.1 | 46.0 | 7.3 % | 31.1 | 13.3 | 0.7920 |

1. **專家 BCE 項不能拿掉。** `--w-bce 0` 全面低於基線，守門 AUC 0.5311 等於擲骰子。
   那 1 097 對只佔訓練對數的 1.4 %，卻是唯一的個體級監督；型別標籤太弱
   （KC 池中位 111 顆同型 EM，InfoNCE 的正例幾乎不設限），
   單靠它撐不起 scratch head——best dev 落在 epoch 5，等於還沒學就到頂。
2. **訓練更久沒有用。** e35 除 ALPN 外全面差於 e10，dev 選擇是對的。
   守門 AUC 幾乎不動（0.8122 → 0.8138）；acc@0.5 確實改善
   （0.6885 → 0.7213，來自多跑的 BCE 步數），但 AUC 卡在 0.81——
   scratch head 在個體級專家任務上就是追不上 annotator head 的 0.905。
3. **ALPN 沒有學起來。** e10 7.3 %、e35 9.8 %，CI 重疊，
   而 median_rank 從基線的 7 退到 9–10，排序整體反而變差。
4. **`--head-init existing` 會讓先驗退化，cross 負例沒有堵住這條路。**
   守門確實最好（0.8561，五個組態之冠）、LC 也保住了（43.3，接近基線的 45.5），
   但 KC 先驗從 0.0 % 衝到 **20.2 %**、整體 20.9 %，是所有組態裡最高的。
   它的 rank-1 34.8 % 因此要大幅打折——其中很大一塊可被
   「完全忽略 FC、只用每顆 EM 平均分」的虛無模型重現。
   扣掉先驗後它在主指標上是**退步**的：MRR 0.458 < 基線 0.464、
   p@5 59.3 << 基線 66.8、median_rank 3 → 4。

**兩個初始化在同一條軸上交換**：`existing` 保住 annotator head 原本擅長的
（LC、守門），代價是把它原本的 EM 側傾向一併保留並放大；
`scratch` 換到更好的排序（MRR、p@5），代價是丟掉 LC 與守門。

5. **完全從頭訓練（`--trunk-init scratch`，不繼承任何 annotator 權重）全面輸給基線。**
   MRR 0.350 vs 0.464、p@5 46.0 vs 66.8、median_rank 6 vs 3。
   ⚠️ 但這個否定有強混淆：dev 最佳只有 0.369（annotator trunk 版本是 0.635），
   760 顆 FC 端到端訓練 conv + head 餵不飽，所以「輸」有相當部分只是資料不足。
   ⚠️ **2026-09-18 補充**：這輪還有另一個障礙。`--tune-trunk` 會把 trunk 的 6 個 BN 鎖在推論模式；從頭訓練時它們的 moving 統計量停在初始值（均值 0、變異數 1），**等於從未做過正規化**，而 annotator 的 BN 是正常訓練的。加上這輪早於三個 bug 的修正，「資料不足」這個解讀不成立，這輪結果不能拿來判斷從頭訓練的能力。
   **分族群的樣態證實了這點**：KC（254 顆，資料最多）rank-1 31.1 基本追平基線的
   32.7 且先驗 0.0 %；垮掉的是小族群 LC（45.5 → 13.3）與 ALPN
   （MRR 0.180 → 0.062、median_rank 7 → 28）。

**六個組態沒有一個通過守門，主指標上也沒有一個乾淨勝過基線。**

### ⭐ EM 先驗的劑量反應

| 繼承多少 annotator | em_prior |
|---|---|
| 基線（完全沒做排序微調） | 3.9 % |
| 完全從頭（什麼都不繼承） | 7.3 % |
| 只繼承 trunk（`head_init=scratch`） | 11.4 % |
| 繼承 trunk + head（`head_init=existing`） | 20.9 % |

兩件事同時成立：**排序微調本身就會增加先驗**（3.9 → 7.3，連什麼都不繼承時都會），
而且**繼承愈多 annotator 增加愈多**。

### 機制假設（未證實）

配合 `analysis_hubness_debias/expert_em_prior.py` 的結果
（專家標註見過的 EM 在全池是**被壓低**的，富集倍數 0.2x —— 先驗不是記住身分），
目前最合理的解釋是：

> **型別層級的正例標籤在結構上無法懲罰「通用好度」。**
> KC 的池裡中位有 111 顆同型 EM，InfoNCE 只要把任何一顆形態泛用的 EM 推高就能得分，
> 它分不出「這顆對這根 FC 是對的」與「這顆對誰都說得過去」。
> 目標函數本身獎勵先驗，換初始化只是換放大倍率。

若成立，該改的不是損失權重也不是初始化，而是**標籤**——而這撞上死結：
唯一的個體級標籤只有 1 097 對，而 `--w-bce 0` 已證明少了它會全盤崩潰。
它既是唯一的解藥，量又不足以當主食。

**怎麼驗證**：只拿池內同型 EM 數量少的 FC 來訓練（同型 EM 愈少，
型別標籤愈接近個體標籤）。若先驗隨同型 EM 數下降而下降，機制就確立。
`analysis_external_validation/results/pair_labels.csv` 的 `n_correct_in_pool`
欄可直接切。**尚未做。**

### ⚠️ 撤回：舊切分的 fold 0 數字

FC 切分改為隨 fold 轉（`split_seed = seed + fold * 1000`）之前，
fold 0 曾得到 **ALPN rank-1 46.4 %** 與 **KC em_prior 41.7 %**。
兩者在新切分下都沒有重現（ALPN 7.3 %／9.8 %，KC 先驗 5.7 %／0.0 %），
而且先後排除了兩個解釋：BCE 造成先驗污染（`--w-bce 0` 否證）、
過訓練造成記憶 EM 傾向（`--pick-epoch 35` 否證）。
**它們是舊切分的產物，不是穩定的估計，不應被引用。**

教訓：家族層級的估計在 n = 41–88 時極不穩定。
任何單折的族群數字在多折／多種子重跑之前都不可採信——
這也適用於 §3.0 引用的階段 2 探針數字。

### 下一步

- ~~**`--head-init existing`**~~ ✅ 已做 —— **no-go**（見上方第 4 點）。
  ⚠️ 本節原本寫「擔心的退化路徑已被 cross 負例堵死」，**這句是錯的**，已刪。
  實測 cross 負例**沒有**堵住它：`existing` 的 KC 先驗 20.2 %，全場最高。
  當初對 `existing` 的疑慮方向是對的，只是當時援引的證據
  （舊切分的 39.4 %）碰巧不可靠。
- ~~**先驗來自專家標註的分布**~~ ❌ **已否證**
  （`analysis_hubness_debias/expert_em_prior.py`）。
  專家訓練集見過的 EM 在全池是**被壓低**的：平均分 Δ 全部顯著為負
  （`pos_only` −0.088 [−0.103, −0.070]、`neg_only` −0.154、`both` −0.202），
  且有單調的劑量反應但方向相反（出現愈多次分數愈低）。
  它們佔候選池 4.26 % 的 EM，只拿下 0.84 % 的 rank-1，**富集倍數 0.2x**。
  先驗不是記住身分（identity shortcut）。
  ⚠️ 該腳本的 `mean_score` 量的是「海綿程度」而非「品質」——
  它是該 EM 在 2 735 顆不相干 FC 上的平均分。所以這張表真正說的是
  「專家標註過的 EM 比池子平均更不海綿」，合理（會送人工判讀的形態較有辨識度）。
- **驗證上面的機制假設**（同型 EM 數分層訓練），見上一節。
- **多折／多種子**：相信任何族群數字的前提。
- **LC 退步的診斷**：LC 是唯一基線本身就帶先驗成分的族群（18.9 %）。
  `scratch` 把 LC 打壞（29.0）而 `existing` 保住（43.3），
  兩者的 LC 先驗卻都升到 35–37 %。需釐清 LC 的基線成績有多少本來就是先驗。

---

## 工作量（更新後）

| 階段 | 項目 | 估計 |
|---|---|---|
| 0 | `build_confident_list.py` 改單用 annotator | 1 小時 |
| ~~1~~ | ~~per-EM 去偏~~ | ✅ 已做，no-go |
| ~~2~~ | ~~凍結 trunk 探針~~ | ✅ 已做，go |
| 3 | 池內三元組取樣（正負皆從池挖、混合策略） | 半天 |
| 3 | 拆 logit + 聯合 train_step（trunk 凍結，純 MLP） | 半天 |
| 3 | 評估腳本參數化 + 守門指標 | 半天 |
| ~~3~~ | ~~訓練與調參~~ | ✅ fold 0 已做，見 §階段 3 結果 |

trunk 可凍結讓階段 3 從原估的 3–4 天降到**約 2 天**。
